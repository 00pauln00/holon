- name: "gc_space_utilization"
  hosts: localhost
  connection: local
  vars:
     recipe_name: "gc_space_utilization"
     parent: None

  tasks:
  - block:
    - name: "{{ recipe_name }}: Create 20GB Partition"
      debug:
        msg: "{{ lookup('s3_handler', 'createPartition', wantlist=True) }}"

    - name: "{{ recipe_name }}: Start minio server"
      vars:
        s3_dir: "minio_data"
      debug:
         msg:
          - "{{ lookup('s3_handler', 'start_s3', s3_dir, wantlist=True) }}"
      when: s3Support == "true"
   

    - name: "Generate DBIs with vblk entry for first chunk"
      vars:
         input_param: {
                  "chunk": "1",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "Generate DBIs with vblk entry for second chunk"
      vars:
         input_param: {
                  "chunk": "2",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 3"
      vars:
         input_param: {
                  "chunk": "3",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "create DBIs with vblk entry for chunk 4"
      vars:
         input_param: {
                  "chunk": "4",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "create DBIs with vblk entry for chunk 5"
      vars:
         input_param: {
                  "chunk": "5",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "create DBIs with vblk entry for chunk 6"
      vars:
         input_param: {
                  "chunk": "6",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 7"
      vars:
         input_param: {
                  "chunk": "7",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 8"
      vars:
         input_param: {
                  "chunk": "8",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 9"
      vars:
         input_param: {
                  "chunk": "9",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 10"
      vars:
         input_param: {
                  "chunk": "10",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 11"
      vars:
         input_param: {
                  "chunk": "11",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 12"
      vars:
         input_param: {
                  "chunk": "12",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "{{ recipe_name }}: Start GCService"
      vars:
        dryRun: False
        delDBO: True
        partition: True  
      debug:
         msg:
          - "{{ lookup('s3_handler', 'start_gcService', dryRun, delDBO, partition, wantlist=True) }}"

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 1."
      vars:
         Chunk: "1"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 2."
      vars:
         Chunk: "2"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 3"
      vars:
         Chunk: "3"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 4"
      vars:
         Chunk: "4"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 5"
      vars:
         Chunk: "5"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 6"
      vars:
         Chunk: "6"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 7"
      vars:
         Chunk: "7"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 8"
      vars:
         Chunk: "8"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 9"
      vars:
         Chunk: "9"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 10"
      vars:
         Chunk: "10"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 11"
      vars:
         Chunk: "11"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 12"
      vars:
         Chunk: "12"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 1"
      vars:
         Chunk: "1"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 2."
      vars:
         Chunk: "2"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 3."
      vars:
         Chunk: "3"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 4."
      vars:
         Chunk: "4"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 5."
      vars:
         Chunk: "5"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 6."
      vars:
         Chunk: "6"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 7."
      vars:
         Chunk: "7"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 8."
      vars:
         Chunk: "8"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 9."
      vars:
         Chunk: "9"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 10."
      vars:
         Chunk: "10"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 11."
      vars:
         Chunk: "11"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 12."
      vars:
         Chunk: "12"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "Get the json path"
      set_fact:
        base_dir="{{ recipe_params['base_dir']}}"
        raft_uuid="{{ recipe_params['raft_uuid']}}"

    - name: "prepare path to read recipe JSON file"
      shell: cat "{{base_dir}}"/"{{raft_uuid}}"/"{{raft_uuid}}".json
      register: result

    - name: "Convert output to JSON format"
      set_fact:
        jsondata: "{{ result.stdout | from_json | default({}) }}"

    - name: "{{ recipe_name }}: Get the GC Service process pid and status."
      set_fact:
        gc_process_pid: "{{ jsondata.gcService_process.process_pid | default('') }}"
        gc_process_status: "{{ jsondata.gcService_process.process_status | default('') }}"
      when: jsondata.gcService_process is defined

    - name: "{{ recipe_name }}: pause the GC process."
      vars:
         pid : "{{ gc_process_pid | int }}"
      debug:
        msg: "{{ lookup('s3_handler', 'pauseGCProcess', pid, wantlist=True) }}"

    - name: "{{ recipe_name }}: create a 18GB file."
      debug:
         msg:
          - "{{ lookup('s3_handler', 'createFile', wantlist=True) }}"

    - name: "update DBIs with vblk entry for chunk 1"
      vars:
         input_param: {
                  "chunk": "1",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "update DBIs with vblk entry for chunk 2"
      vars:
         input_param: {
                  "chunk": "2",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "update DBIs with vblk entry for chunk 3"
      vars:
         input_param: {
                  "chunk": "3",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "unpdate DBIs with vblk entry for chunk 4"
      vars:
         input_param: {
                  "chunk": "4",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "update DBIs with vblk entry for chunk 5"
      vars:
         input_param: {
                  "chunk": "5",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "update DBIs with vblk entry for chunk 6"
      vars:
         input_param: {
                  "chunk": "6",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"
    
    - name: "create DBIs with vblk entry for chunk 7"
      vars:
         input_param: {
                  "chunk": "7",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 8"
      vars:
         input_param: {
                  "chunk": "8",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 9"
      vars:
         input_param: {
                  "chunk": "9",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 10"
      vars:
         input_param: {
                  "chunk": "10",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 11"
      vars:
         input_param: {
                  "chunk": "11",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "create DBIs with vblk entry for chunk 12"
      vars:
         input_param: {
                  "chunk": "12",
                  "maxPunches": "1024",
                  "maxVblks": "2097152",
                  "punchAmount": "6000",
                  "punchesPer": "0",
                  "maxPunchSize": "2048",
                  "seed": "1",
                  "seqStart": "0",
                  "vbAmount": "2097152",
                  "vblkPer": "100000",
                  "genType": "1",
                  "blockSize": "4",
                  "blockSizeMax": "32",
                  "startVblk": "0",
                  "strideWidth": "",
                  "overlapSeq": "",
                  "numOfSet": "",
                  "vdev": "643eef86-e42b-11ee-8678-22abb648e432",
                  "s3configPath": "",
                  "punchwholechunk": "false",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'example_params', input_param, wantlist=True) }}"

    - name: "{{ recipe_name }}: Resume the GC process."
      vars:
         pid : "{{ gc_process_pid | int }}"
      debug:
        msg: "{{ lookup('s3_handler', 'resumeGCProcess', pid, wantlist=True) }}"

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 1."
      vars:
         Chunk: "1"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 2"
      vars:
         Chunk: "2"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 3"
      vars:
         Chunk: "3"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 4"
      vars:
         Chunk: "4"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 5"
      vars:
         Chunk: "5"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 6"
      vars:
         Chunk: "6"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 7"
      vars:
         Chunk: "7"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 8"
      vars:
         Chunk: "8"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 9"
      vars:
         Chunk: "9"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 10"
      vars:
         Chunk: "10"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 11"
      vars:
         Chunk: "11"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker file present for chunk 12"
      vars:
         Chunk: "12"
         mType: "Both"
         stage: "wait_for_gc_marker"
         marker_seq: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
      debug:
        msg: "Waiting for GC Marker File {{ marker_seq }}"
      until: marker_seq[0] is not none
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 1."
      vars:
         Chunk: "1"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 2."
      vars:
         Chunk: "2"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 3."
      vars:
         Chunk: "3"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 4."
      vars:
         Chunk: "4"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 5."
      vars:
         Chunk: "5"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2
    
    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 6."
      vars:
         Chunk: "6"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 7."
      vars:
         Chunk: "7"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 8."
      vars:
         Chunk: "8"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 9."
      vars:
         Chunk: "9"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 10."
      vars:
         Chunk: "10"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 11."
      vars:
         Chunk: "11"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2

    - name: "{{ recipe_name }}: Wait until GC marker and NISD marker gets match for chunk 12."
      vars:
         Chunk: "12"
         mType: "Both"
         Sequence: "{{ lookup('s3_handler', 'GetSeqOfMarker', mType, Chunk, wantlist=True) }}"
         gcMarkerEndSeq: "{{ Sequence[0] }}"
         NisdMarkerEndSeq: "{{ Sequence[1] }}"
      debug:
        msg: "Wait until marker values gets match {{ gcMarkerEndSeq }} and {{ NisdMarkerEndSeq }}"
      until: gcMarkerEndSeq == NisdMarkerEndSeq
      retries: 300
      delay: 2
