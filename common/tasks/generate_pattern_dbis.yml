---

    - name: "Generate DBIs."
      vars:
         input_param: {
                  'genType' : "{{ Type }}",
                  'overlapSeq' : "{{ OverlapSeqNum | default(0) }}",
                 }
      debug:
         msg:
          - "{{ lookup('s3_handler', 'generate_pattern', input_param, wantlist=True) }}"

