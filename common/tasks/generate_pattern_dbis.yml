---

    - name: "Generate DBIs."
      vars:
         input_param: {
                  'genType' : "{{ Type }}",
                 }
         s3Support: "{{ s3_support }}"
      debug:
         msg:
          - "{{ lookup('s3_handler', 'generate_pattern', input_param, s3Support, wantlist=True) }}"

