---

    - name: "Starting client {{ ClientUUID }}"
      debug:
       msg:
         - "{{lookup('niova_kvclient', Operation, Key, Value, OutfileName, NumRequest, MultiKey, Sequential, wantlist=True)}}"

